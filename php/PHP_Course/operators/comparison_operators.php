<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="">
    </head>
    <body>
    <?php
    // Comparison Operators
    // == --- equal to
    // > --- greater than
    // >= --- greater than or equal to
    // <  --- less than
    // <= --- less than or equal to
    // <> --- Not equal to
    // === --- is identical to (same value and same data type)
    // !== --- is not identical to
    // <=> (the Spaceship operator) --- if L > R then 1, if L == R then 0, if L < R then -1
    
    $x = 15.0;
    $y = 15;

    if ($x !== $y) {
        echo "This is true";
    }
    else {
        echo "This is false";
    }


    ?>
        
        <script src="" async defer></script>
    </body>
</html>